// import { Ticket, TicketStatusType } from "./tickets";

// it("return version number in response of ticket creation", async () => {
//   const ticket = await axios

//   expect(ticket.version).toEqual(0);
// });
